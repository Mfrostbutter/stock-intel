# Template only. Do NOT commit real values. Create the live Secret at deploy time, e.g.
#   kubectl -n intraday create secret generic intraday-recorder-secrets \
#     --from-literal=STOCKS_INTRADAY_SVC_PASSWORD="$PW" \
#     --from-literal=ALPACA_API_KEY_ID="$KID" \
#     --from-literal=ALPACA_API_SECRET_KEY="$KS"
apiVersion: v1
kind: Secret
metadata:
  name: intraday-recorder-secrets
  namespace: intraday
type: Opaque
stringData:
  STOCKS_INTRADAY_SVC_PASSWORD: "<intraday service role password>"
  ALPACA_API_KEY_ID: "<Alpaca key id>"
  ALPACA_API_SECRET_KEY: "<Alpaca secret key>"
